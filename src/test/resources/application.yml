# Test profile: usa mesmo Postgres (localhost). Rode docker compose antes dos testes
# ou use -Dspring.profiles.active=test com banco disponível.
spring:
  datasource:
    url: psql 'psql 'postgresql://neondb_owner:npg_1gGaTxcdrtP4@ep-wispy-bar-adxt9h5n-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require'
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

jwt:
  secret: test-secret-with-at-least-32-characters-long-for-hs256
  expiration-ms: 3600000
